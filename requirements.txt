# Core
pydantic>=2.0
python-dotenv>=1.0

# LLM clients (we already know these work)
google-generativeai>=0.7
huggingface_hub>=0.24
groq>=0.10

# Instructor — Pydantic-aware LLM wrapper (we'll use this in Milestone 6)
instructor>=1.0

# Datasets — Hugging Face data loading (Milestone 7)
datasets>=2.20

# Observability
langfuse>=3.0

# CLI
click>=8.1
rich>=13.0